# georgiosrizos.github.io

Personal website built with Jekyll + GitHub Pages.

Used a few Claude tokens to make this look better.

## Local preview (optional)

```bash
gem install bundler
bundle install
bundle exec jekyll serve
# → open http://localhost:4000
```
